on:
  workflow_call:
    inputs:
      version:
        required: true
        type: string

jobs:
  installer:
    name: build installer + upload as artifact (${{ matrix.os }})
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [macos-11, windows-2019, ubuntu-18.04]

    steps:
      - uses: actions/checkout@v3

      - name: Setup VM for cifuzz
        uses: ./.github/actions/vm_setup
        with:
          os: ${{ matrix.os }}

      - name: Build installer
        run: |
          make version=${{ inputs.version }} installer

      - name: Upload installer
        uses: actions/upload-artifact@v3
        with:
          name: cifuzz_installer
          path: build/bin/cifuzz_installer_*
          if-no-files-found: error
          retention-days: 5
